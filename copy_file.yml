---
- name: Copy files to remote server
  hosts: all
  become: true
  tasks:
    - name: Copy file to remote server
      copy:
        src: /tmp/az_connect_linux.sh
        dest: /tmp
      become_user: root
      become_method: su
