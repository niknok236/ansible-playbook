---
- name: net/ipv4/conf/all/accept_redirects = 0
  hosts: ubuntu
  become: true
  tasks:
    - apt:
        update_cache: yes
        upgrade: full
        autoremove: yes
      become_user: root
      become_method: su
