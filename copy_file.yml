---
- name: Copy files to remote server
  hosts: ubuntu
  become: true
  tasks:
    - name: Copy file to remote server
      copy:
        src: /tmp/semaphore/test.txt
        dest: /tmp/
        owner: semaphore
        group: semaphore
        mode: '0644'
