---
- name: Check local path
  hosts: all
  become: true
  tasks:
    - name: checking and copying
      shell:
          "pwd > /home/ruhsadin/mypath.txt"
      become_user: root
      become_method: su
  tasks:
    - name: Fetching
      fetch: 
        src: /home/ruhsadin/mypath.txt
        dest: /home/ruhsadmin/myohmy.txt
        remote_src: false
      become_user: root
      become_method: su
